28. to check nubmer is divisible by 3.


psudeo: 
   input num
   if num%3==0 then true,else false

pseudo: routine 
    function IsDivisibleBy3(num)
        if num % 3 == 0
            return True 
        else 
            return False 
      
pseudo: routine test 
    function TestIsDivisibleBy3()
        input num 
        if IsDivisibleBy3(num)
            print "Divisible By 3"
        else 
            print "Not Divisible By 3"
